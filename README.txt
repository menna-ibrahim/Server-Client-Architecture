Server-Client Architecture
--------------------------
Server receives http 1.0 and 1.1 requests and sends the appropriate responses
Client sends http 1.1 requests to server
--------------------------------------------------------------------------------------
Server supports multithreaded connections, multiplexing, pipelining and timeout
Client has a cache in main memory which it saves the packets received from the client
